import SubSystemSelection from './SubSystemSelection';

export default function SubSystemSelectionV1() {
  // V1 preserves the original responsive behavior (mobile/tablet vs desktop).
  return <SubSystemSelection />;
}

